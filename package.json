{
  "name": "leap-surgery-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "립모션 기반 수술 시뮬레이션 체험 데모 (웹 / Three.js)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.4.0"
  }
}
